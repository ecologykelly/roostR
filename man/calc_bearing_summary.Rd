% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/restlessness.R
\name{calc_bearing_summary}
\alias{calc_bearing_summary}
\title{Summarise antenna bearing during the roost interval}
\usage{
calc_bearing_summary(
  data,
  id_col = "recvDeployName",
  doy_col = "doy",
  time_col = "time",
  roost_col = "roost_time",
  leave_col = "leave_roost_time",
  bearing_col = "antBearing"
)
}
\arguments{
\item{data}{A dataframe of Motus detections with roost timing and bearing
columns, typically after calling \code{\link[=add_roost_times]{add_roost_times()}}.}

\item{id_col}{Character. Individual identifier column. Default \code{"recvDeployName"}.}

\item{doy_col}{Character. Day-of-year column. Default \code{"doy"}.}

\item{time_col}{Character. POSIXct timestamp column (UTC). Default \code{"time"}.}

\item{roost_col}{Character. Roost onset time column. Default \code{"roost_time"}.}

\item{leave_col}{Character. Departure time column. Default \code{"leave_roost_time"}.}

\item{bearing_col}{Character. Antenna bearing column (degrees, 0–360).
Default \code{"antBearing"}.}
}
\value{
A dataframe with one row per \code{doy_col} (nights with a complete roost
interval and bearing data), containing:
\itemize{
\item \code{doy_col}
\item \code{mean_bearing}: circular mean bearing in degrees (0–360).
\item \code{mode_bearing}: modal bearing, or \code{NA} if no unique mode.
}
}
\description{
For each day-of-year, computes the circular mean and modal bearing of
detections falling within the biological night interval. Useful for
identifying which direction the roosting animal is oriented relative to
the receiver array.
}
\details{
The circular mean uses vector averaging over angles converted to radians.
The mode returns \code{NA} when there is no unique most-frequent bearing.
}
\examples{
\dontrun{
bearing_summary <- calc_bearing_summary(sp)
}

}
