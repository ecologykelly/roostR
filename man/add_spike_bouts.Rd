% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/restlessness.R
\name{add_spike_bouts}
\alias{add_spike_bouts}
\title{Annotate spike bouts in the full detection dataframe}
\usage{
add_spike_bouts(
  data,
  id_col = "tagDeployID",
  doy_col = "doy",
  time_col = "time",
  roost_col = "roost_time",
  leave_col = "leave_roost_time",
  spike_col = "sig.diff",
  spike_threshold = 4,
  gap_min = 2
)
}
\arguments{
\item{data}{A dataframe of Motus detections with roost timing columns.}

\item{id_col}{Character. Individual identifier column. Default \code{"tagDeployID"}.}

\item{doy_col}{Character. Day-of-year column. Default \code{"doy"}.}

\item{time_col}{Character. Timestamp column (POSIXct UTC). Default \code{"time"}.}

\item{roost_col}{Character. Roost onset time column. Default \code{"roost_time"}.}

\item{leave_col}{Character. Departure time column. Default \code{"leave_roost_time"}.}

\item{spike_col}{Character. Activity column to threshold. Default \code{"sig.diff"}.}

\item{spike_threshold}{Numeric. Values above this are spikes. Default \code{4}.}

\item{gap_min}{Numeric. Gaps greater than this many minutes between spikes
begin a new bout. Default \code{2}.}
}
\value{
The input dataframe with additional columns:
\itemize{
\item \code{interval_start}, \code{interval_end}: roost interval bounds (POSIXct).
\item \code{in_roost_interval}: logical, \code{TRUE} if detection falls within the
roost interval.
\item \code{spike}: logical, \code{TRUE} if \code{spike_col > spike_threshold} and within
the roost interval.
\item \code{dt}: minutes since the previous detection within the same
individual × day group.
\item \code{new_bout}: \code{1L} if this spike starts a new bout, \code{0L} otherwise.
\item \code{bout_id}: integer bout identifier; \code{NA} for non-spike rows.
}
}
\description{
Like \code{\link[=calc_restless_all]{calc_restless_all()}}, but instead of returning a summary, this
function adds spike and bout columns directly to the detection-level
dataframe. Useful for plotting individual restlessness events or for
further row-level filtering.
}
\details{
Spike and bout detection logic is identical to \code{\link[=calc_restless_all]{calc_restless_all()}}.
Only detections within the biological night interval are flagged; all
other rows receive \code{spike = FALSE} and \code{bout_id = NA}.
}
\examples{
\dontrun{
sp <- add_spike_bouts(sp, spike_threshold = 4, gap_min = 2)

library(ggplot2)
ggplot(sp, aes(x = time, y = sig.diff)) +
  geom_point(color = "grey70", size = 0.7) +
  geom_point(data = subset(sp, spike), color = "red", size = 1.5) +
  geom_hline(yintercept = 4, linetype = 2, color = "blue")
}

}
\seealso{
\code{\link[=calc_restless_all]{calc_restless_all()}} for the summary version.
}
