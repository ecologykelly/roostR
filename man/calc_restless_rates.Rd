% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/restlessness.R
\name{calc_restless_rates}
\alias{calc_restless_rates}
\title{Compute restlessness rates normalized by observation time}
\usage{
calc_restless_rates(
  data,
  id_col = "tagDeployID",
  doy_col = "doy",
  bouts_col = "n_bouts",
  restless_min_col = "total_restless_min",
  observed_hr_col = "observed_time_hr"
)
}
\arguments{
\item{data}{A dataframe with restlessness and observation columns, typically
after joining outputs of \code{\link[=calc_restless_all]{calc_restless_all()}} and
\code{\link[=compute_night_observation]{compute_night_observation()}} back to the detection data.}

\item{id_col}{Character. Individual identifier column. Default \code{"tagDeployID"}.}

\item{doy_col}{Character. Day-of-year column. Default \code{"doy"}.}

\item{bouts_col}{Character. Bout count column from \code{\link[=calc_restless_all]{calc_restless_all()}}.
Default \code{"n_bouts"}.}

\item{restless_min_col}{Character. Total restless time column (minutes).
Default \code{"total_restless_min"}.}

\item{observed_hr_col}{Character. Observed time column (hours) from
\code{\link[=compute_night_observation]{compute_night_observation()}}. Default \code{"observed_time_hr"}.}
}
\value{
A dataframe with one row per individual × day-of-year, containing
the input columns plus:
\itemize{
\item \code{total_restless_hr}: \code{total_restless_min / 60}.
\item \code{restless_per_obs_hr}: bouts per observed hour (\code{NA} if
\code{observed_time_hr} is 0 or \code{NA}).
\item \code{prop_time_restless}: proportion of observed roost time spent restless
(\code{NA} if \code{observed_time_hr} is 0 or \code{NA}).
}
}
\description{
Normalizes restlessness metrics from \code{\link[=calc_restless_all]{calc_restless_all()}} by the amount of
time the animal was actually observed during the roost interval (from
\code{\link[=compute_night_observation]{compute_night_observation()}}). This corrects for nights where the receiver
only detected the animal for part of the roost period.
}
\examples{
\dontrun{
restless_rates <- calc_restless_rates(sp)
}

}
