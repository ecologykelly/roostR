% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roost_detection.R
\name{detect_roost_departure}
\alias{detect_roost_departure}
\title{Detect roost departure time near sunrise}
\usage{
detect_roost_departure(
  data,
  id_col = "recvDeployName",
  time_col = "time",
  doy_col = "doy",
  sunrise_col = "Sunrise",
  sigdiff_col = "sig.diff",
  window_before = 90,
  window_after = 120,
  roll_width = 10,
  spike_threshold = 4,
  median_threshold = 3
)
}
\arguments{
\item{data}{A dataframe of Motus detections with a signal difference column.}

\item{id_col}{Character. Individual identifier column. Default \code{"recvDeployName"}.}

\item{time_col}{Character. POSIXct timestamp column (UTC). Default \code{"time"}.}

\item{doy_col}{Character. Day-of-year column. Default \code{"doy"}.}

\item{sunrise_col}{Character. POSIXct sunrise column (UTC). Default \code{"Sunrise"}.}

\item{sigdiff_col}{Character. Signal difference column from
\code{\link[=add_signal_diffs]{add_signal_diffs()}}. Default \code{"sig.diff"}.}

\item{window_before}{Numeric. Minutes before sunrise to start the search
window. Default \code{90}.}

\item{window_after}{Numeric. Minutes after sunrise to end the search window.
Default \code{120}.}

\item{roll_width}{Integer. Number of observations in the left-aligned
(forward) rolling median. Default \code{10}.}

\item{spike_threshold}{Numeric. \code{sigdiff_col} must exceed this to qualify as
an activity spike. Default \code{4}.}

\item{median_threshold}{Numeric. Forward rolling median must exceed this
value simultaneously with the spike. Default \code{3}.}
}
\value{
A dataframe with one row per (\code{id_col}, \code{doy_col}) combination where
a departure was detected, containing columns \code{id_col}, \code{doy_col}, and
\code{leave_roost_time} (POSIXct UTC).
}
\description{
Finds the time each individual first shows sustained activity after roosting,
interpreted as departure from the roost. The algorithm:
\enumerate{
\item Restricts detections to a window from \code{window_before} minutes before
sunrise to \code{window_after} minutes after sunrise.
\item Computes a left-aligned (forward) rolling median over \code{roll_width}
observations.
\item Flags detections where signal difference exceeds \code{spike_threshold} AND
the forward rolling median exceeds \code{median_threshold}.
\item Returns the earliest such detection per individual × day.
}
}
\details{
Default thresholds are tuned to dark-eyed junco data. Adjust \code{spike_threshold}
and \code{median_threshold} for other species or transmitter types.
}
\examples{
\dontrun{
leave_times <- detect_roost_departure(sp)
}

}
