% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{wrap_hours_overnight}
\alias{wrap_hours_overnight}
\title{Wrap UTC hour-of-day values for overnight plotting}
\usage{
wrap_hours_overnight(
  data,
  pivot_hour = 17,
  hour_cols = c("hour", "sunset_hour", "sunrise_hour", "roost_hour", "leave_roost_hour")
)
}
\arguments{
\item{data}{A dataframe containing one or more numeric hour-of-day columns.}

\item{pivot_hour}{Numeric. UTC hour that becomes the left edge of the plot
axis. Hours strictly less than \code{pivot_hour} are shifted +24.
Default \code{17} (≈ noon Eastern Standard Time).}

\item{hour_cols}{Character vector. Names of columns to wrap. Columns not
present in \code{data} are silently skipped. Default wraps \code{"hour"},
\code{"sunset_hour"}, \code{"sunrise_hour"}, \code{"roost_hour"}, and
\code{"leave_roost_hour"}.}
}
\value{
The input dataframe with additional \verb{*_wrap} columns for each
column in \code{hour_cols} that exists in \code{data}. For example, \code{"hour"}
produces a new \code{"hour_wrap"} column.
}
\description{
Shifts hour-of-day values so that a chosen UTC pivot hour becomes the left
edge of a plot axis. This is useful for visualizing overnight detections
(e.g. dusk to dawn) without a break at midnight. Hours before the pivot
are increased by 24 so they appear to the right of midnight on the axis.
}
\details{
The default \code{pivot_hour = 17} corresponds to 17:00 UTC ≈ noon EST, placing
noon at the left edge and producing a noon-to-noon view.
}
\examples{
\dontrun{
overnight <- wrap_hours_overnight(sparrow_processed, pivot_hour = 17)
}

}
