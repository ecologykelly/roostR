% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{prep_sun_times}
\alias{prep_sun_times}
\title{Read and parse a sunrise/sunset CSV into UTC times}
\usage{
prep_sun_times(path, ref_year, tz_local = "America/New_York")
}
\arguments{
\item{path}{Character. Path to the sunrise/sunset CSV file.}

\item{ref_year}{Integer. The calendar year of the detection data. Used to
reconstruct full dates from \code{J.day} so that returned POSIXct values are
in the correct year. Derive with
\code{as.integer(format(min(data$time), "\%Y"))}.}

\item{tz_local}{Character. Local timezone of the input time strings.
Default \code{"America/New_York"}.}
}
\value{
A dataframe with one row per \code{J.day} and POSIXct UTC columns for
each solar event present in the input file.
}
\description{
Reads a CSV of daily solar event times (one row per day of year), parses
the time strings into POSIXct UTC, and returns a clean lookup table ready
for joining to detection data with \code{\link[=add_sun_times]{add_sun_times()}}.
}
\details{
The CSV must contain a \code{J.day} column (integer day of year, 1–366) and at
least \code{Sunrise} and \code{Sunset} columns with time strings in 12-hour format
(e.g. \code{"7:48 AM"}). Optional twilight columns (\code{AT.Start}, \code{AT.End},
\code{NT.Start}, \code{NT.End}, \code{CT.Start}, \code{CT.End}) are parsed if present.
A \code{Day} column (date string) is accepted but not required — dates are
reconstructed from \code{J.day} and \code{ref_year} to ensure the correct year.
}
\examples{
\dontrun{
sun <- prep_sun_times("data-raw/sunrisetimes.csv", ref_year = 2025)
}

}
