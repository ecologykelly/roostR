% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diel.R
\name{add_day_night}
\alias{add_day_night}
\title{Classify detections as day or night}
\usage{
add_day_night(
  data,
  time_col = "time",
  sunrise_col = "Sunrise",
  sunset_col = "Sunset"
)
}
\arguments{
\item{data}{A dataframe of Motus detections containing timestamp,
sunrise, and sunset columns.}

\item{time_col}{Character. POSIXct timestamp column (UTC). Default \code{"time"}.}

\item{sunrise_col}{Character. POSIXct sunrise column (UTC). Default
\code{"Sunrise"}.}

\item{sunset_col}{Character. POSIXct sunset column (UTC). Default \code{"Sunset"}.}
}
\value{
The input dataframe with one new column:
\itemize{
\item \code{diel}: factor with levels \code{"day"} and \code{"night"}.
}
}
\description{
Labels each detection as \code{"day"} or \code{"night"} based on per-row sunrise and
sunset times. A detection is "day" if its timestamp falls on or after
sunrise and before sunset on the same UTC date; all other detections are
"night". The resulting column is a factor with levels \code{c("day", "night")}.
}
\details{
All times (\code{time}, \code{Sunrise}, \code{Sunset}) must be POSIXct in UTC.
}
\examples{
\dontrun{
# After joining sunrise/sunset times to your detections:
sp <- add_day_night(sp)
table(sp$diel)
}

}
