% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{add_continuity_flags}
\alias{add_continuity_flags}
\title{Flag observation gaps and assign run IDs}
\usage{
add_continuity_flags(
  data,
  id_col = "tagDeployID",
  time_col = "time",
  time_diff_col = "time.diff",
  threshold = 2
)
}
\arguments{
\item{data}{A dataframe of Motus detections with a \code{time.diff} column.}

\item{id_col}{Character. Individual identifier column. Default \code{"tagDeployID"}.}

\item{time_col}{Character. Timestamp column. Default \code{"time"}.}

\item{time_diff_col}{Character. Elapsed-time column in minutes, from
\code{\link[=add_signal_diffs]{add_signal_diffs()}}. Default \code{"time.diff"}.}

\item{threshold}{Numeric. Gap threshold in minutes. Detections within this
window are marked as continuous. Default \code{2}.}
}
\value{
The input dataframe with three new columns:
\itemize{
\item \code{continuous}: \code{1} if \code{time.diff <= threshold}, \code{0} otherwise (first
detection per individual is set to \code{0}).
\item \code{gap}: logical, \code{TRUE} if \code{time.diff > threshold}.
\item \code{run.id}: integer run identifier that increments each time \code{continuous}
changes value (i.e., each contiguous detection run has a unique ID).
}
}
\description{
Marks gaps between consecutive detections and segments the detection series
into continuous runs. A "continuous" observation is one where the gap to the
previous detection is within \code{threshold} minutes. The default 2-minute
threshold is tuned to Motus transmitters pinging every 19–26 seconds; adjust
for other hardware.
}
\details{
Requires \code{\link[=add_signal_diffs]{add_signal_diffs()}} to have been called first (to produce
\code{time.diff}).
}
\examples{
\dontrun{
sp <- collapse_motus_time(sparrow52550)
sp <- add_signal_diffs(sp)
sp <- add_continuity_flags(sp, threshold = 2)
}

}
